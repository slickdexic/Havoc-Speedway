{
  "name": "@havoc-speedway/shared",
  "version": "1.0.0",
  "description": "Shared types and constants for Havoc Speedway",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/**/*.ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
